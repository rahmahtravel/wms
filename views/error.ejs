<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Rahmah Travel</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root { --primary-color: #f9a825; --primary-light: #ffd54f; --primary-dark: #f57f17; }
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
  </style>
</head>
<body class="bg-gray-50 h-screen flex items-center justify-center">
  <div class="text-center px-4 max-w-2xl">
    <i class="fas fa-times-circle text-8xl text-red-500 mb-6"></i>
    <h1 class="text-4xl font-bold text-gray-800 mb-4">Terjadi Kesalahan</h1>
    <p class="text-xl text-gray-600 mb-8">Maaf, terjadi kesalahan pada server.</p>
    <% if (error && error.message) { %>
      <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-8 text-left">
        <p class="text-red-800 font-mono text-sm"><%= error.message %></p>
        <% if (error.stack) { %>
          <pre class="text-red-600 text-xs mt-4 overflow-auto"><%= error.stack %></pre>
        <% } %>
      </div>
    <% } %>
    <a href="/" class="inline-flex items-center px-6 py-3 rounded-lg text-white font-semibold transition" style="background-color: var(--primary-dark);">
      <i class="fas fa-home mr-2"></i>
      Kembali ke Dashboard
    </a>
  </div>
</body>
</html>
